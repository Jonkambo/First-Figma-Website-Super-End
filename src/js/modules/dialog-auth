import A11yDialog from 'a11y-dialog'

const requestDialogAuth = () => {
    const authContainer = document.querySelector('#dialog-auth')

    if (!authContainer) return

    const dialogAuth = new A11yDialog(authContainer)
}

export default requestDialogAuth